#Write a program to calculate speed using distance and time.

distance = float(input("Enter the distance (in kilometers): "))
time = float(input("Enter the time (in hours): "))

speed = distance / time
print("Speed:", speed, "kilometers per hour")